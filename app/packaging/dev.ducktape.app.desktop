[Desktop Entry]
# The Linux half of duck:// scheme registration. `MimeType` is what makes
# xdg-open (and every browser that defers to it) hand a
# `duck://<chain>/<program>/…` address to this app; `%u` is what puts the
# URL in argv. Whoever installs it substitutes @EXEC@ with the absolute
# path of `ducktape-app` — a desktop session does not inherit a shell's
# PATH.
#
# macOS is NOT covered here: it registers a scheme through the bundle's
# CFBundleURLTypes plus an open-URL event, neither of which a .desktop file
# reaches.
#
# The file's name and `StartupWMClass` are both `dev.ducktape.app`, the id each
# native window declares through its WindowOptions in `app/src/shell.rs`.
# Keep the X11 WM_CLASS / Wayland app_id aligned with this desktop entry;
# the binary's name is not the application id.
Type=Application
Name=Ducktape
GenericName=Consensus workspace
Comment=A local-first consensus workspace for people and agents
Exec=@EXEC@ %u
Icon=ducktape
Terminal=false
Categories=Network;Development;Office;
MimeType=x-scheme-handler/duck;
StartupWMClass=dev.ducktape.app

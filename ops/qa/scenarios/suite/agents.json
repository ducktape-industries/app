{
 "name": "agents",
 "about": "Switch between the agent registry and runs. Starts from an open console and leaves it open.",
 "include": [
  "setup.json"
 ],
 "gaps": [
  "ducktape-views@a2a73e1 agents/src/lib.rs:1201 — the executor picker is a PickList with no label",
  "ducktape-views@a2a73e1 agents/src/lib.rs:487-545 — run rows are named only by their opaque run id"
 ],
 "steps": [
  {
   "say": "open the Agents tab in the rail",
   "settle_ms": 4000,
   "expect": "the Agents tab is selected and the agents view shows an Agents label and Registry and Runs buttons with Registry checked, and agent rows or No agents registered; no failure state (no failure title such as 'This network does not list this view', no Retry button) and it is not still loading"
  },
  {
   "say": "press Runs",
   "expect": "Runs reports checked; run rows or No runs yet show"
  },
  {
   "say": "press Registry",
   "expect": "Registry reports checked"
  }
 ]
}
